---
const { as = "div", class: classnames = "", ...attrs } = Astro.props;
const Tag = as;
---

<Tag class={`w-full lg:w-2/3 mt-6 ${classnames}`} {...attrs}>
    <p class="text-2xl font-thin">
        <slot />
    </p>
</Tag>
